### For client side devlopment

```sh
git clone https://github.com/mustardfrog/competition-temlpate.git
cd competition-temlpate
git checkout frontend

# push after made changes
git push origin frontend
```

### Devlopment setup

```sh
cd client
npm install
npm run dev
```
